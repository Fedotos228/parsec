

export default function ProjectContent() {
  return (
    <div className='grid grid-cols-1 lg:grid-cols-2 group'>
      {/* <Image
        src={content.image}
        alt={content.alt}
        width={500}
        height={500}
        className='h-lvh w-full object-cover group-even:order-2'
      />
      <div className='flex items-center justify-center p-4 bg-neutral-800'>
        <div className='max-w-[500px]'>
          {content.body.map((item, index) => (
            <div key={index} className='mb-4'>
              {item.type === 'text' && <Paragraph type='lg' className='font-hidi'>{item.text}</Paragraph>}
              {item.type === 'list' && (
                <ul>
                  {item.list?.map((listItem, listIndex) => (
                    <li key={listIndex}>{listItem}</li>
                  ))}
                </ul>
              )}
            </div>
          ))}
        </div>
      </div> */}
    </div>
  )
}